# ZCode Switcher 优化功能测试计划

## 测试目标

验证以下两个优化功能：

1. **账号切换前检查所有余额**：确保只有当候选账号的所有量纲（token 和积分）都高于阈值时，才会切换到该账号
2. **暂停执行模式**：在余额低于阈值时，可选择暂停所有执行而非自动切换

## 前置条件

- [ ] 应用已构建并安装最新版本
- [ ] 至少配置 2 个 GLM-5/2 账号
- [ ] 至少一个账号有多个套餐入口（Start Plan 和 Coding Plan）
- [ ] 本地网关已启用（用于暂停模式测试）
- [ ] ZCode 已配置使用本地网关

## 测试场景

### 场景 1：套餐内切换优先

**目的**：验证当前套餐入口余额不足时，会先尝试切换到同账号的另一个套餐

**步骤**：
1. 在账号 A 中，设置 Start Plan 入口的 token 阈值为 50000
2. 设置 Coding Plan 入口的 token 阈值为 50000
3. 确保账号 A 的 Start Plan 余额低于阈值，Coding Plan 余额高于阈值
4. 启用自动切换，设置低额度动作为"自动切换套餐/账号"
5. 触发一次模型请求

**预期结果**：
- 系统检测到 Start Plan 余额不足
- 系统切换到 Coding Plan（同账号）
- 不会切换到账号 B

**验证点**：
- [ ] Toast 提示显示"切换到套餐 Coding Plan"
- [ ] 当前入口显示为 Coding Plan
- [ ] 仍然使用账号 A

### 场景 2：所有套餐都低于阈值才切换账号

**目的**：验证只有当账号内所有套餐都低于阈值时，才会切换账号

**步骤**：
1. 确保账号 A 的所有套餐（Start Plan 和 Coding Plan）余额都低于阈值
2. 账号 B 的至少一个套餐余额高于阈值
3. 启用自动切换
4. 触发一次模型请求

**预期结果**：
- 系统检测到账号 A 所有套餐余额都不足
- 系统切换到账号 B
- 使用账号 B 中余额充足的套餐

**验证点**：
- [ ] Toast 提示显示切换账号
- [ ] 当前账号显示为账号 B
- [ ] 使用的套餐余额高于阈值

### 场景 3：候选账号必须所有量纲都满足阈值

**目的**：验证候选账号的 token 和积分都必须高于各自阈值

**准备**：
1. 设置 token 阈值为 50000，积分阈值为 5
2. 准备账号 B：token 余额 60000（高于阈值），积分余额 2（低于阈值）
3. 准备账号 C：token 余额 60000（高于阈值），积分余额 10（高于阈值）
4. 账号 A 所有套餐余额都低于阈值

**步骤**：
1. 启用自动切换
2. 触发一次模型请求

**预期结果**：
- 系统跳过账号 B（积分不足）
- 系统切换到账号 C（token 和积分都满足）

**验证点**：
- [ ] 不会切换到账号 B
- [ ] 切换到账号 C
- [ ] Toast 提示显示切换到账号 C

### 场景 4：暂停模式 - 拦截请求

**目的**：验证暂停模式下会拦截模型请求

**步骤**：
1. 确保本地网关已启动（端口 17860）
2. 确保 ZCode 配置使用网关（`http://localhost:17860/v1`）
3. 设置低额度动作为"暂停执行"
4. 确保当前账号/套餐余额低于阈值
5. 启用自动切换
6. 在 ZCode 中发送一个模型请求

**预期结果**：
- 系统检测到余额不足
- 系统调用 `setQuotaGuard(true)` 启用网关拦截
- Toast 提示"余额低于阈值，已暂停"
- 模型请求被网关拦截，返回错误

**验证点**：
- [ ] Toast 显示暂停提示
- [ ] 模型请求失败
- [ ] 错误信息提示余额不足
- [ ] 不会自动切换账号或套餐

### 场景 5：暂停模式 - 手动恢复

**目的**：验证暂停后需要手动操作才能恢复

**步骤**：
1. 延续场景 4 的暂停状态
2. 手动切换到余额充足的账号
3. 再次发送模型请求

**预期结果**：
- 切换账号后，网关拦截被清除
- 模型请求成功执行

**验证点**：
- [ ] 切换账号后 toast 提示成功
- [ ] 模型请求不再被拦截
- [ ] 请求正常返回结果

### 场景 6：切换模式不会拦截

**目的**：验证切换模式下不会启用网关拦截

**步骤**：
1. 设置低额度动作为"自动切换套餐/账号"
2. 确保有可切换的候选账号
3. 当前账号余额低于阈值
4. 触发一次模型请求

**预期结果**：
- 系统自动切换到候选账号
- 不会调用 `setQuotaGuard(true)`
- 模型请求正常执行

**验证点**：
- [ ] 自动切换成功
- [ ] 模型请求不被拦截
- [ ] 请求正常返回结果

## 测试数据准备

### 账号配置示例

```json
{
  "accounts": [
    {
      "name": "账号 A",
      "plans": {
        "Start Plan": {
          "token": 30000,
          "points": 3
        },
        "Coding Plan": {
          "token": 70000,
          "points": 8
        }
      }
    },
    {
      "name": "账号 B", 
      "plans": {
        "Start Plan": {
          "token": 60000,
          "points": 2
        }
      }
    },
    {
      "name": "账号 C",
      "plans": {
        "Start Plan": {
          "token": 60000,
          "points": 10
        }
      }
    }
  ]
}
```

### 阈值配置

- Token 阈值：50000
- 积分阈值：5

## 测试环境

- 操作系统：Windows 10/11
- ZCode 版本：最新版
- zcode-switcher 版本：1.1.14
- 本地网关端口：17860

## 成功标准

- [ ] 所有 6 个测试场景都通过
- [ ] 套餐内切换优先于账号切换
- [ ] 候选账号的所有量纲都必须满足阈值
- [ ] 暂停模式正确拦截请求
- [ ] 切换模式不会拦截请求
- [ ] UI 提示准确清晰
- [ ] 无异常错误或崩溃

## 测试记录

| 场景 | 执行时间 | 结果 | 备注 |
|------|----------|------|------|
| 场景 1 | | | |
| 场景 2 | | | |
| 场景 3 | | | |
| 场景 4 | | | |
| 场景 5 | | | |
| 场景 6 | | | |
